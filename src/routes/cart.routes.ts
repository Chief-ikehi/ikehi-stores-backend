import { Router } from "express"; import { addToCart, clearCart, getCart, removeFromCart, updateCartItem } from "../controllers/cart.controller"; import { authenticate } from "../middlewares/auth.middleware"; import { validateRequest } from "../middlewares/validation.middleware"; import { AddToCartDto, RemoveFromCartDto, UpdateCartItemDto } from "../validators/cart.validator"; const router = Router(); router.use(authenticate); router.get("/", getCart); router.post("/add", validateRequest(AddToCartDto), addToCart); router.put("/update", validateRequest(UpdateCartItemDto), updateCartItem); router.delete("/remove", validateRequest(RemoveFromCartDto), removeFromCart); router.delete("/clear", clearCart); export default router;
